const FooterSection = () => {
  return (
    <footer className="px-6 md:px-12 py-6 border-t border-border flex items-center">
      <span className="font-mono text-[16px] font-semibold text-white">
        branchy<span className="text-primary">.</span>
      </span>
      <span className="font-mono text-[11px] text-ghost ml-4">
        // turn chaos into clarity
      </span>
    </footer>
  );
};

export default FooterSection;
